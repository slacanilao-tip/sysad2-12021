---
# tasks file for roles/Ubuntu/user_docker

 - name: Create docker group
   group:
     name: docker
     state: present

 - name: Add docker group to user
   user:
     name: samuel
     group: docker


